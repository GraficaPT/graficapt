{
  "name": "graficapt",
  "scripts": {
    "build": "node scripts/generate-env.mjs && node scripts/build-products.mjs",
    "prebuild": "node scripts/generate-env.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.42.3",
    "@vercel/analytics": "^1.5.0"
  }
}