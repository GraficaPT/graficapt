{
  "name": "graficapt",
  "scripts": {
    "build": "node scripts/generate-env.mjs && node scripts/prerender-products.mjs",
    "prebuild": "node scripts/generate-env.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.42.3",
    "@vercel/analytics": "^1.5.0"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}